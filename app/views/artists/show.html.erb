<h1><%= @artist.name %></h1>
<ul>
  <% @artist.songs.each do |song| %>
    <li><%= link_to @artist.name artist_song_path(@artist, song) %></li>
  <% end %>
</ul>

@artist.name, href: artist_songs_path(@artist)